<Window x:Class="video.Views.PlayerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Video Player"
        Height="450"
        Width="800"
        MinHeight="300"
        MinWidth="400"
        xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- header -->
            <RowDefinition Height="*"/>
            <!-- player -->
            <RowDefinition Height="Auto"/>
            <!-- navigation -->
        </Grid.RowDefinitions>

        <!-- header -->

        <Border Grid.Row="0" Background="LightGray">
            <Menu ItemsSource="{Binding MenuItems}"
          Background="Transparent"
          BorderThickness="0">

                <Menu.ItemContainerStyle>
                    <Style TargetType="MenuItem">
                        <Setter Property="Header" Value="{Binding Title}" />
                        <Setter Property="ItemsSource" Value="{Binding Children}" />
                        <Setter Property="Command" Value="{Binding Command}" />
                        <Setter Property="FontSize" Value="15" />
                        <Setter Property="Padding" Value="6,2" />
                    </Style>
                </Menu.ItemContainerStyle>

            </Menu>
        </Border>

        <!-- player -->
        <Grid Grid.Row="1" Background="Black">
            <!--<Label x:Name="TitleLbl" Content="Media player" Padding="5,0,5,5" Margin="20" FontWeight="SemiBold" FontSize="16" BorderBrush="DodgerBlue" BorderThickness="0,0,0,2"/>-->
            <MediaElement x:Name="Player"
                          LoadedBehavior="Manual"
                          UnloadedBehavior="Manual"
                          Stretch="Uniform"/>
        </Grid>

        <!-- navigation -->
        <Border Grid.Row="2"
                Background="LightGray"
                Padding="4.5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            Margin="0,0,8,0">
                    <Button Name="PlayPauseBtn" Click="TestBtn_Click" Width="70" Height="30">⏯</Button>
                    <Button Name="BackBtn" Click="TestBtn_Click" Width="40" Height="30" Margin="8,0,0,0">⏮</Button>
                    <Button Name ="StopBtn" Click="TestBtn_Click" Width="70" Height="30" Margin="8,0,0,0">⏹</Button>
                    <Button Name ="ForwardBtn" Click="TestBtn_Click" Width="40" Height="30" Margin="8,0,0,0">⏩</Button>
                </StackPanel>

                <Slider Name="ProgressSlider" 
                        Grid.Column="1"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Stretch"
                        Margin="12,0"/>

                <TextBlock Name="ProgressTimer"
                           Grid.Column="2"
                           VerticalAlignment="Center"
                           Margin="8,0"
                           Foreground="Black"
                           Text="00:00 / 00:00"/>

                <Slider Name="VolumeSlider" 
                        Grid.Column="3"
                        Width="100"
                        VerticalAlignment="Center"
                        Margin="12,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
