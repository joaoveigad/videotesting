<Window x:Class="video.Views.PlayerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Video" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Vídeo -->
            <RowDefinition Height="Auto"/>
            <!-- Controles -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0">
            <Grid Background="LightGray">
                <TextBlock Text="Header"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="1"/>
            </Grid>
        </Border>

        <!-- Vídeo -->
        <Grid Grid.Row="1" Background="Black">
            <MediaElement x:Name="VideoPlayer"
                          LoadedBehavior="Manual"
                          UnloadedBehavior="Manual"
                          Stretch="Uniform"/>
        </Grid>

        <!-- Controles -->
        <Border Grid.Row="2" Background="LightGray" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Width="70" Height="30">⏯</Button>
                    <Button Width="40" Height="30" Margin="8,0,0,0">⏮</Button>
                    <Button Width="70" Height="30" Margin="8,0,0,0">⏹</Button>
                    <Button Width="40" Height="30" Margin="8,0,0,0">⏩</Button>
                </StackPanel>

                <Slider Grid.Column="1" VerticalAlignment="Center" Margin="12,0"/>

                <TextBlock Grid.Column="2"
                           VerticalAlignment="Center"
                           Margin="8,0"
                           Foreground="White"
                           Text="00:00 / 00:00"/>

                <Slider Grid.Column="3"
                        Width="100"
                        VerticalAlignment="Center"
                        Margin="12,0"/>
            </Grid>
        </Border>

    </Grid>
</Window>
